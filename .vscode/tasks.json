{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "g++ -g main.cpp src/*.cpp -o main.out -lpthread",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}